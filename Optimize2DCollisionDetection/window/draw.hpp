#pragma once
#include"../shape/shape.hpp"

namespace my
{
	template<typename CollisionDetectionPolicy>
	void draw(const std::vector<shape>& shape, float left, float right, float bottom, float top)
	{

	}
}